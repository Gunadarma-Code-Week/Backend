# Example environment file for GCW backend
# Copy this to `.env` and replace placeholders with real values (do NOT commit real secrets).

ENVIRONMENT=local
VERSION=1

APP_NAME=GCW
PORT=8000
MODE=debug
HOST_HEADER=localhost:${PORT}

# CORS: frontend origin allowed to access this API
#CORS_ORIGIN=https://your-frontend.example.com
CORS_ORIGIN=http://localhost:3000

# Database (Postgres)
DB_CONNECTION=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=gcw_db
DB_USER=gcw_user
DB_PASS=changeme

# API prefixes
API_BASE_URL=/api/v1/gcw/resources
ADMIN_API_BASE_URL=/admin/api/v1/gcw/resources

# JWT (used by service/jwt-service.go)
JWT_SECRET=change_me_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_ISSUER=gcw

# Email (used by service/email-service.go)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-email-password
EMAIL_FROM=no-reply@yourdomain.com

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# DomJudge integration (optional)
DOMJUDGE_URL=https://domjudge.example.com
DOMJUDGE_CONTEST_ID=1
DOMJUDGE_USERNAME=domjudge_user
DOMJUDGE_PASSWORD=domjudge_password
