### untuk api post, put, dan delete (jika berhasil di eksekusi tanpa error). api response:
```json
{
  "message": "string",
  "data": "interface{}"
}
```

### untuk api get (jika). api response:
```json
{
  "data": "interface{}",
}
```